// A file that imports cleanly and defines ZERO tests. This is the fixture for
// the per-file zero-count meta-control: under a total-only check a file like
// this is indistinguishable from one that was never written, which is the shape
// of a silently-deleted suite. Driven by test/fs/runnerFlush.test.mjs.
//
// Deliberately empty. Do not add a test here.
export const INTENTIONALLY_NO_TESTS = true;
