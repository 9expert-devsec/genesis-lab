/**
 * Stub for `@/lib/actions/masterclass-outlines` in the render tier — reached
 * by MasterclassOutlineUpload (and MasterclassCourseFormClient). Same policy as
 * the other action stubs: throw rather than resolve.
 */
export async function signMasterclassOutlineUpload() {
  throw new Error('stub-masterclass-outline-actions: signMasterclassOutlineUpload must not be called in a render test');
}
